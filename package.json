{
  "name": "hugoapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd UI && npm start",
    "server": "cd server && npm start",
    "start": "concurrently --kill-others --success last 'npm run server' 'npm run client'"
  },
  "dependencies": {
    "nodemon": "^1.19.2"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
